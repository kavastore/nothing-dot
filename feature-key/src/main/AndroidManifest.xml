<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <!-- In-app wireless ADB connects to the local adbd over the loopback/Wi-Fi socket. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Needed to read enabled-state of the Essential Space packages on Android 11+ (package visibility). -->
    <queries>
        <package android:name="com.nothing.ntessentialspace" />
        <package android:name="com.nothing.ntessentialrecorder" />
    </queries>

    <application>
        <service
            android:name=".service.EssentialKeyAccessibilityService"
            android:exported="false"
            android:label="@string/key_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/essential_key_service_config" />
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>
